{
    "name": "scheduled-task",
    "version": "1.0.0",
    "description": "Scheduled Lambda task triggered by EventBridge",
    "main": "index.js",
    "scripts": {
      "test": "jest",
      "lint": "eslint .",
      "build": "rm -rf dist && mkdir dist && cp -r src/* dist/ && cd dist && npm install --production && zip -r ../function.zip ."
    },
    "devDependencies": {
      "eslint": "^8.56.0",
      "jest": "^29.7.0"
    }
  }